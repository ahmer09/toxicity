/*
 * ToxicityPage Messages
 *
 * This contains all the text for the ToxicityPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ToxicityPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ToxicityPage container!',
  },
});
